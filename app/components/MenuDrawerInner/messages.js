/*
 * MenuDrawerInner Messages
 *
 * This contains all the text for the MenuDrawerInner component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MenuDrawerInner';

export default defineMessages({
  selectLanguage: {
    id: `${scope}.selectLanguage`,
    defaultMessage: 'Select Language',
  },
});
