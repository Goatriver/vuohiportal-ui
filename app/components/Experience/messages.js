/*
 * Experience Messages
 *
 * This contains all the text for the Experience component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Experience';

export default defineMessages({
  jobHomepage: {
    id: `${scope}.jobHomepage`,
    defaultMessage: 'Employer homepage',
  },
});
